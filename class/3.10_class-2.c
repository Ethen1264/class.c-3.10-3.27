#include <stdio.h>

int main()
{
    /*int age, kor, eng, math;
    char grade(A~E), gender(M / F); */

    //1. 나이가 17살 이고 성별이 남자인 경우 
    //(age==17) && (gander==.M.)
    //2. 등급이 A, B, C중 하나인 경우 
    // grade=='a' || grade == b





    return 0;
}
